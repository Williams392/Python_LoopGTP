#---------------------------------------------------------------

. Paso 1 -> en Consola: (Crear el ENTORNO VIRTUAL -> venv), url -> https://docs.python.org/3/library/venv.html
# comando -> [python3 -m venv venv], se crear la carpeta -> [venv]

#---------------------------------------------------------------


. Paso 3 -> activar el modo virtual en consola:
# comando -> [source venv/bin/activate]

#---------------------------------------------------------------

. Paso 3 -> Instalar Django:
# comando -> [pip install Django]

#---------------------------------------------------------------


. Paso 4 -> Para guardar una lista de paquetes instalados en Python:
# comando -> [pip freeze > requirements.txt]

#---------------------------------------------------------------

. Paso 4 -> Para guardar una lista de paquetes instalados en Python:
# comando -> [pip freeze > requirements.txt]


#---------------------------------------------------------------

. Paso 5 -> core -  la prinicipal:
# comando -> [django-admin startproject core]

#---------------------------------------------------------------
#---------------------------------------------------------------
#---------------------------------------------------------------

_ SQL lite, creo?
# Adentro de -> archivo core:

. paso :
# comando -> [clear] 

#---------------------------------------------------------------
# archivo core -> siempre correr aca:

. Paso : para CORRER a navegador:
# comando -> [python manage.py runserver]

#---------------------------------------------------------------
_ Esto al Ultimo:

# archivo core -> siempre correr aca:

. Paso : Gener uno archivo de los cambios:
1.
# comando -> [python manage.py makemigrations]
2.
# comando -> [python manage.py migrate]

#---------------------------------------------------------------
#---------------------------------------------------------------
# archivo core -> siempre correr aca:

. crear el super usuario(Django administration -> pagina):

# comando -> [python manage.py createsuper]
_ Username: 
_ Password: 
_ contra:
